#include <new>

#include <MdtKea.h>
#include <keaMatrix.hpp>

/* This is only implementable on PS2 at the moment, so by by default it should do nothing */
void keaMatrix :: writebackMatrixChol()
{
}

/* This is only implementable on PS2 at the moment, so by by default it should do nothing */
void keaMatrix :: prefetchMatrixChol()
{
}
